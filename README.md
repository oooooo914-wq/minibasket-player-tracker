# MiniBasket Player Tracker

AndroidのChromeで、スマートフォン内の試合動画から指定した1人を追跡するPWAです。
**現段階は選手追跡MVPです。走行距離・コート座標・正確な出場時間は計算しません。**

公開URL: https://oooooo914-wq.github.io/minibasket-player-tracker/

## MVP 0.6：画像の連続性で追う方式へ変更

0.5の待ち時間調整だけでは追跡の実用性が不足していたため、追跡に使う証拠と再生処理を変更しました。**実際のミニバス動画で改善した割合・実機速度はまだ測定できていません。** 顔や背番号で本人を認識するモデルを導入したものではありません。

- **特徴点の連続追跡**：選んだ子の胴体付近で最大48点を取り、画像ピラミッドを使うLucas–Kanade法で前後方向に追跡します。往復位置誤差・画像の差・点の生存割合・分布を検査し、途切れた点を他の場所で勝手に取り直しません。点の補充は、人物検出が一致して他の枠と重ならない時だけ行います。
- **一時的な検出抜け**：特徴点の連続性と外見が残る間は水色の「画像追跡」を継続します。人物検出なしの画像追跡は最長6秒。その後、または特徴点も失った場合は、最後の証拠から設定した1〜3秒だけ黄色点線の予測保留となります。予測位置は軌跡に加えません。
- **密集時の対応付け**：追っていた点が候補のどちらに入ったかを評価します。両方の枠に入る点は本人を区別する根拠として数えません。十分な点が途切れず1人に属する時だけ、位置と色だけの曖昧さを解消できます。点が切れた同じ服の競合は再指定を求めます。
- **弱い検出も条件付きで利用**：検出スコア0.18〜0.35の人物枠を直前の位置・外見・画像の連続性で裏付けられる時だけ使います。新しい選手を選ぶ画面には0.35以上だけ表示。低スコア枠への切替を無条件に許す変更ではなく、ByteTrackの考え方を参考にした対象1人用の補助です。ByteTrack全体や全選手のID管理は未実装です。
- **背景の補正**：選手枠の外から最大120点を追い、外れ値を除いた相似変換でパン・小さなズーム／回転を補助します。点数・画面内の分布が足りない補正は採用しません。コート座標や距離への変換ではありません。
- **連続再生を標準に**：0.5の毎コマ停止・シークを標準から外し、通常の動画再生と並行して追跡します。指定速度は上限で、解析時間に応じて0.1倍〜指定速度の範囲で調整し、「実際の再生速度」に表示します。能力不足なら遅くなり、結果が古すぎる場合は停止します。以前の方式は「コマ送り解析」として残しています。
- **GPUを先に試す**：MediaPipeのGPU指定で初期化・試験推論を行い、失敗時は閉じてCPUで再試行します。GPUが必ず速いとは保証せず、診断には使用方式・AI推論時間・特徴点処理時間を分けて記録します。実行中のGPU障害は停止し、再準備が必要です。
- **画面外の区間**：ロスト後は「画面外・不明のまま再生」で欠測のまま進められます。別人を対象にせず、戻ってきたら停止して確認します。「直前のロストへ戻る」で見失った0.5秒前に移動できます。全試合の区間編集・逆方向追跡・長い画面外からの自動再識別は未実装です。

## 継続する安全策と診断

保留からの復帰には異なる時刻の3回連続確認（0.18秒以上）が必要です。停止後の自動復帰はありません。選択時の色分布は固定し、更新用の色分布も低スコア検出では更新しません。短い競合が解消しても、本人を区別する証拠がなければ選び直します。

「診断ログを保存」でJSONを端末に保存、「診断ログを表示」で長押しコピーできます。動画内時刻・処理時間・遅れ・特徴点数・候補ごとの棄却条件等を直近1,000件まで保持します。集計は判定フレーム数で、実際の失敗回数ではありません。動画・画像・ファイル名・色分布そのものを含めず、自動送信もしません。

青い枠の「候補1」等はその停止画面で選ぶための番号です。**背番号OCRは未実装です。**

合計54テストで、合成画像の20px移動、特徴点の断絶、重複点の除外、背景の相似変換、弱い検出の条件、人物検出が抜けた時の継続と期限、Workerの統合・GPU失敗時のCPU再試行、再生とPWAを検証しています。これらは実試合の本人追跡精度やAndroid性能を保証しません。

## 既存実装から継続する機能

既存のHTML / CSS / JavaScript / MediaPipe / canvas構成を継続しています。フレームワークへの置き換えや作り直しはしていません。

- ローカル動画をObject URLで開く。動画のアップロード・API課金・アカウント登録なし。
- 再生／停止、全体シーク、±1秒、±0.1秒、0.25／0.5／1倍再生。
- 一時停止中・時間移動後にも人物検出。枠タップまたは番号ボタンで1人を選択。
- キャンバスと再生操作を分離し、枠が再生ボタンを遮らないよう修正。
- 旧MediaPipe `0.10.22`のJS URLが404だったため、取得できる`0.10.21`へJS/WASMを揃えて固定。
- 初期画面の外部AI依存を外し、モデル読込に失敗しても動画UIを利用・再試行可能。
- AI専用のWebGLキャンバスを用意し、初回推論が成功してから「準備完了」を表示。
- 人物検出と軽量追跡をWeb Workerに移動。解析画像は長辺768px以下、同時処理は1枚、フレーム待ち行列なし。
- 人物検出5／7／10 fps（動画時間基準、上限）。画像追跡は実時間15 fps以下。重い端末では処理時間に応じて間引く。初期再生の上限1倍、検出7 fps。
- 定速度Kalman Filter、予測位置、枠の大きさと重なり、胴体／パンツ別の色分布による外見比較。
- 画像の特徴点をピラミッドLucas–Kanade法で追跡。旧ブロックフロー関数は比較用の回帰テストに残しています。
- 選択時の外見を固定アンカーとして保持し、他人への段階的な特徴のずれを抑制。
- 許容時間を超える未確認、解消できない候補競合、処理遅延で停止。**見失い状態からは自動復帰せず、タップでの再指定が必要。**
- 緑の枠と足元マーカー、直近8秒の画面座標の軌跡。履歴は最大600点。
- 時間移動・動画変更では追跡を破棄。古いWorker応答を世代番号で無効化。バックグラウンドでは停止。
- PWAアイコン192／512px、相対`start_url`／`scope`、アプリ専用Service Worker。

## Androidだけでの使い方

1. 公開URLをChromeで開き、画面上部が **MVP 0.6** であることを確認。「更新して再読込」が出たら押してください。旧版のままなら関連タブとアプリをすべて閉じ、開き直します。「動画を選ぶ」でスマホ内の動画を選びます。
2. 初回のAI準備を待つ。通信が必要なのはアプリ・モデル・WASMの取得です。
3. シークまたは±ボタンで選手が見える位置に移動。停止画面に青い番号付き枠が出ます。
4. 対象の枠か番号をタップし、緑の枠を確認。「再生」を押します。
5. 初期設定の「連続追跡（標準）」で再生。「実際の再生速度」で端末に応じた速度を確認できます。追いつかない場合は「コマ送り解析」に切り替えます。
6. 最初は「予測保留の上限：2秒」で確認。黄色点線は本人未確認です。取り違えに気付いたらすぐ停止して選び直す。自動停止した場合も、枠から再指定します。重なっている場合は±ボタンで離れた位置に移動します。
7. 画面外の間は「画面外・不明のまま再生」で通過できます。対象が戻ったら停止して確認してください。誤った区間へ戻るには「直前のロストへ戻る」。
8. 問題が起きた直後に「診断ログを保存」。そのJSONを開発者に渡すと直前の判定を調べられます。動画を選び直すとログはリセットされます。
9. Chromeメニューの「ホーム画面に追加」（端末によっては「アプリをインストール」）でPWAとして利用。

まず選手が大きく映る10〜30秒程度の区間から確認してください。重い場合は0.25倍／検出5 fps。動画形式はChromeが再生できるもの（例：MP4 H.264）。HEVC等は端末依存です。別アプリに移ると追跡を停止します。

## プライバシーと長尺動画

`File`を`URL.createObjectURL`で動画要素に渡します。動画全体の`arrayBuffer()`化や全フレーム展開は行いません。前後2枚分の縮小画像ピラミッド、現在の解析画像、上限付き特徴点・軌跡・診断ログを保持し、転送したImageBitmapは閉じます。動画変更時には古いObject URLを破棄します。

動画・画像・外見特徴をネットワークへ送るコード、アクセス解析、外部推論APIはありません。第三者への通信はjsDelivrとGoogleのモデル配信に限ります（ページ配信はGitHub Pages）。Service Workerはアプリ画面用ファイルだけをキャッシュし、動画や他のアプリのキャッシュには触れません。

20〜50分動画を逐次処理する設計ですが、実機の長時間動作・発熱・ブラウザのデコードメモリは未検証です。再読込すると動画選択・追跡結果は消えます。全試合の結果保存は未実装です。

## 現時点の限界

- 同じユニフォーム・交差・小さな人物・遮蔽・ベンチの観客・ぶれで検出失敗や取り違えが起こり得ます。停止判定は保守的ですが、誤追跡ゼロを保証しません。
- 色分布は簡易外見特徴で、高度なRe-ID／背番号認識ではありません。
- 特徴点追跡も、繰り返し模様・ぶれ・小さな人物・完全な遮蔽では誤対応し得ます。背景補正は平面的な相似変換のみで、強い遠近変化・大きなズーム／回転・カット切替には弱く、コートへの射影は未実装です。
- **黄色い軌跡は画面上の履歴です（黄色点線の枠は未確認の予測）。カメラ移動を含み、実際の走行経路・距離ではありません。**
- 実際の試合動画は今回提供されていないため、連続追跡時間や実動画精度は未検証です。
- 初回モデル取得はオンライン必須。アプリ画面はオフライン起動できますが、モデル／WASMはService Workerで完全保存していないため、オフラインでのAI起動は保証しません。
- 性能が不足する場合、実測検出頻度は設定値より低くなります。数値は実時間あたりの検出回数で、0.5倍再生では動画時間基準の設定値より小さくなります。

## GitHub Pagesの公開設定（スマートフォンで1回だけ）

2026-09-12に利用者がPagesを有効化し、公開ページの表示を確認しました。以下は別の環境で公開する場合の設定です。

1. GitHubのこのリポジトリを開き、Settings → Pages。
2. Build and deployment の Source を **Deploy from a branch** にする。
3. Branchを **main**、フォルダを **/(root)** にしてSave。
4. デプロイ完了後に公開URLを開く。

ビルド不要の静的配信です。`.nojekyll`あり。既存のroot配信を継続します。Pages向けパスはすべて相対指定、Workerも`import.meta.url`を基準に解決します。AIのJS/WASMは同じ固定バージョンの絶対CDN URL、モデルはGoogleの絶対URLです。

更新時は動画確認を終えてアプリと関連タブをすべて閉じ、再度開いてください。新Service Workerは実行中の解析を強制更新しません。更新用ファイルはHTTPキャッシュを再利用せず取得し、新版のキャッシュに旧ファイルが混ざることを防ぎます。

初回インストールでは更新ボタンを出さず、自動でアプリ用キャッシュを有効にします。既存版からの更新だけボタンを表示し、別タブで更新が完了した場合も古いボタンを残しません（0.6公開確認で見つかった表示不具合を修正）。

## 開発・検証

Node.js 22以上、外部npm依存なし。

```sh
npm run check
npm test
python -m http.server 8000
```

最後のコマンドは開発者用であり、利用者はスマホだけで利用できます。GitHub Actionsでも構文・回帰テストを実行します。

- `app.js`: 元の動画選択／描画／操作を拡張。1フレームずつWorkerへ転送。
- `detector-worker.js`: MediaPipeと追跡処理。WASMローダーの`importScripts`に合わせClassic Workerを使用。
- `tracker.js`: Kalman、外見比較、弱い検出と特徴点の証拠の統合、曖昧候補排除。
- `motion.js`: 特徴点抽出、前後Lucas–Kanade、背景相似変換、候補への点の帰属。
- `vendor/`: 固定コミットのJSFeatを同梱（MIT）。Worker／ES module向けの包装変更のみ。出典とライセンスを同梱。
- `ai-config.js`: JS/WASMバージョンとモデルの一元管理。
- `tests/`: 合成人物による追跡・見失い固定・競合・不連続・既知パン、およびPWAの範囲とパス。

実動画の追跡精度はこれらの単体テストでは検証できません。

2026-09-12の公開確認：GitHub Pagesのページ・JS・Worker・manifest・Service Worker・アイコンはHTTP 200。Workブラウザで動画選択、8秒のテストMP4読込、シーク、再生操作、PWAインストール案内の表示を確認しました。検証用ブラウザはWebGL 2非対応のため、人物検出・連続追跡のブラウザ実動作は未検証です。AI初期化ではWebGL 2とウォームアップ推論を確認し、実行不能なのに準備完了と表示しないようにしました。Android Chromeでの実動画確認が必要です。

2026-09-13のMVP 0.5公開確認：Workブラウザで旧0.4から更新ボタンによる反映、動画選択と8秒MP4の読込、＋1秒シーク、保留上限の変更を確認。診断JSONは単体テストで生成内容を検証しましたが、Workブラウザではダウンロード完了を確認できませんでした。ページ・アプリJS・Worker・manifest・Service Worker・アイコンはHTTP 200。アプリ由来の未処理JavaScriptエラーは確認されませんでした。WorkブラウザのWebGL 2非対応によるAI実行の制約は引き続きあり、実試合での改善度は未検証です。

2026-09-23のMVP 0.6公開確認：ページ・JS・Worker・追加した特徴点処理とJSFeat・manifest・Service Worker・アイコンの計12ファイルがHTTP 200で、GitHubの公開コミットと一致。Workブラウザで0.6の表示、8秒MP4の選択、＋1秒シーク、通常再生・停止、診断JSONの表示を確認しました。アプリ由来の未処理JavaScriptエラーは確認されませんでした。今回のWorkブラウザもWebGL 2を利用できず、AI初期化はその制約を画面に表示して停止。人物検出・連続追跡の実動作、Androidでの速度と実試合の精度は未検証です。

## 次の開発

1. 診断ログと実際に見失った場面を照合し、検出失敗・探索範囲・外見変化・候補競合・処理遅れを切り分ける。実動画で「正しく追えた秒数」「誤追跡」「再指定回数」を比較してから調整する。
2. 手動修正区間の保存、追跡履歴の端末内保存・再開。
3. コート線・背景特徴からパン／ズーム／射影変換を推定し、カメラ由来の移動を除去。
4. 背番号や必要に応じた軽量Re-IDを追加して同じユニフォームの区別を改善。
5. コート座標と欠測区間を検証してから距離計算。欠測を推定距離で埋めない。

参考: [JSFeat](https://inspirit.github.io/jsfeat/) / [ByteTrack](https://github.com/FoundationVision/ByteTrack) / [BoT-SORT](https://github.com/NirAharon/BoT-SORT) / [MediaPipe Object Detector for Web](https://ai.google.dev/edge/mediapipe/solutions/vision/object_detector/web_js)
